<%= form_for @clase, :html => { :class => 'form-horizontal' } do |f| %>
<% if @clase.errors.any? %>
<ul>
    <div class="form-group">
     <div class="col-lg-2"></div>
      <div class="col-lg-6">
  <% @clase.errors.full_messages.each do |msg| %>
              <li class="text-danger"> <%= msg %></li>
  <% end %>
   </div>
  </div>
</ul>
<% end %>
  <div class="form-group">
    <%= f.label 'Nombre ', :class => 'control-label col-lg-2' %>
     <div class="col-lg-6">
    <%= f.text_field :nombre, :class => 'form-control' %>
    </div>
  </div>

 <div class="form-group">
    <%= f.label 'Descripción ', :class => 'control-label col-lg-2' %>
     <div class="col-lg-6">
    <%= f.text_area :descripcion, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <div class = 'control-label col-lg-2' ></div>
     <div class="col-lg-6">
      <%= f.submit 'Guardar', :class => 'btn btn-primary' %>
    <%= link_to 'Cancelar',
                clases_path, :class => 'btn' %>
    </div>
  </div>

  <div class="form-actions">
    
  </div>
<% end %>
